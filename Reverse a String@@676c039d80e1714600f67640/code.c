#include <stdio.h>
#include <string.h>
int main(){
    char star[25];
    scanf("%s",&star);
    int l = strlen(star);
    int i;
    for(i=l-1;i>=0;i--){
        printf("%c",str[i]);
    }
}
